# This project requires Python 3.11
# It will not work with Python 3.13+ as PyTorch is not compatible
# NumPy must be <2.0 for compatibility with PyTorch
numpy<2.0
matplotlib>=3.5.0
pandas>=1.3.0
torch>=1.10.0
torchvision>=0.11.0
python-ternary>=1.0.0
typing-extensions>=4.0.0
tqdm>=4.62.0
# Optional visualization libraries
seaborn>=0.11.0 